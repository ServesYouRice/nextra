# Nextra logo concepts

Five alternative logo directions for Nextra, all drawn on the app's existing palette
(accent `#5b8def`, light `#a9c4f7`, deep `#4a6fd4`) so any of them drops into the
current UI without a restyle. Open `preview.html` in a browser to compare them on
dark and light backgrounds at full, navbar (32 px), and favicon (16 px) sizes.

| # | File | Idea |
|---|------|------|
| 1 | `concept-1-signal-n.svg` | **Signal N** — the N's diagonal becomes a transmission: three dots growing as the signal travels from host to viewer. Reads as an N first, a broadcast second. Very robust at tiny sizes. |
| 2 | `concept-2-stream-ribbon.svg` | **Stream Ribbon** — closest evolution of the current mark: one continuous monoline stroke draws the N in a single unbroken gesture (the stream), with broadcast arcs where it arrives. |
| 3 | `concept-3-portal-tile.svg` | **Portal Tile** — rounded screen tile with the N in negative space. The mark *is* an app icon, fitting a product launched as `Nextra.exe`; ideal for taskbar/PWA/social avatar. |
| 4 | `concept-4-broadcast-frame.svg` | **Broadcast Frame** — a monitor with the share arrow bursting out through the corner: your screen leaving the room. The most literal "share your screen with a link" story. |
| 5 | `concept-5-crosscast.svg` | **Crosscast X** — leans on the ✕ instead of the N: two arrows fanning out from one origin (one host → many viewers, plus remote control flowing back). In the wordmark it replaces the x: ne✕tra. |

These are proposals only — nothing in the app references them yet. If one direction
wins, the follow-up is to regenerate `public/brand/` and `public/icons/` from it.
