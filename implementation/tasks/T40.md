# T40 — Cover WHEP connection and disconnect lifecycle timers

<model>
Model: `claude-sonnet-5`
Effort: `high`
Card: T40 in `../KANBAN.md`
Depends on: none
</model>

<role>
You are a Node.js lifecycle test engineer controlling time and EventEmitter
state deterministically. Prove each first-signal transport/producer event either
cancels the correct reaper or closes the WHEP session exactly once.
</role>

<context>
A successful WHEP POST registers an initial 30-second DTLS-connect deadline and
an ICE-disconnected 30-second reaper. DTLS connected cancels the first; ICE
connected/completed cancels the second. DTLS failed/closed and consumer
producerclose close immediately. None has direct route coverage.

Use Node test-context timers:
`t.mock.timers.enable({ apis: ['setTimeout'] })` and `tick(ms)`. Start the HTTP
server before enabling timers; local fetch works on the repository's Node 22
runtime when only `setTimeout` is mocked.

Repeated ICE `disconnected` replacement is deliberately T42. Keep this
assertion-only card on first disconnect, recovery, and terminal events.
</context>

<files>
Read: `lib/whepRoutes.js` — session timers and EventEmitter handlers
Read: `tests/whepRoutes.test.js` — valid success harness
Read: `lib/sessionRegistry.js` — active lookup
Read: `lib/rooms.js` — room cleanup
Create: `tests/whepRoutesLifecycle.test.js` — fake-time lifecycle tests
Do not touch: `lib/whepRoutes.js` — assertion-only task
Do not touch: `tests/whepRoutesSession.test.js` — T39 owns allocation cleanup
Do not touch: `tests/whepRoutesIceDisconnect.test.js` — T42 owns repeats
</files>

<task>
Create a fresh successful video-only session per case, drive its fake transport
and consumer lifecycle under mocked time, and assert both registries, timer
fields, viewer count, and exact close counts after each transition.
</task>

<steps>
- Copy minimal SDP/server/room/fake shapes. Harness returns Location id, room
  session, fake media, and close counters.
- Start listener with real timers. Enable mocked `setTimeout` before POST.
- Never connected: after 201, tick 29,999ms (still present), then 1ms (both maps
  remove, viewer zero, resources close once).
- DTLS connects: emit connected, assert `dtlsConnected=true` and timer null;
  tick 30,000ms and assert session remains.
- Table-test DTLS failed and closed with fresh sessions; cleanup is immediate and
  emitting the same event again does not increase counts.
- ICE recovery: after DTLS connected, emit disconnected, tick 29,999ms, emit
  connected, assert timer null, tick past old deadline and remain. Repeat with
  completed in a fresh row.
- ICE timeout: after DTLS connected, emit disconnected, tick 30,000ms, assert
  exact cleanup.
- Producer close: emit `producerclose`; assert immediate, idempotent cleanup.
</steps>

<constraints>
- Scope: create only `tests/whepRoutesLifecycle.test.js`.
- No real waits, polling, or timer sleeps; advance MockTimers only.
- Fresh room/router/media/server/timer context per row.
- Assert room-local and singleton removal, viewer count, timer fields, and exact
  closes.
- Do not inspect Timeout private fields or invoke callbacks manually.
- Mock neither Date nor setInterval.
- Do not change timeout constants, add injection, edit source, or test repeated
  disconnect here.
</constraints>

<acceptance_criteria>
- Connect deadline closes at 30,000ms, not 29,999ms.
- DTLS connected permanently cancels it; failed/closed clean immediately once.
- ICE connected/completed cancel one pending reaper; uninterrupted disconnect
  closes at 30,000ms.
- Producer close cleans immediately and idempotently.
- Every closure clears both maps/timers, viewer count, and resources exactly once.
- No wall-clock wait is used.
- `git diff --name-only` lists only `tests/whepRoutesLifecycle.test.js`.
</acceptance_criteria>

<verification>
1. `node --test tests/whepRoutesLifecycle.test.js` — expected: pass; MockTimers
   experimental warning is not failure.
2. `node --test --experimental-test-coverage tests/whepRoutesLifecycle.test.js`
   — expected: pass; record route percentages and confirm lifecycle ranges run.
3. `node --test tests/whepRoutes.test.js tests/rooms.test.js` — pass.
4. `node --test` — expected: full suite passes.
5. `npm run lint` — expected: pass.
</verification>

<escalate_if>
- Local fetch cannot complete with only setTimeout mocked after listener start.
- A deadline fires early/survives late, or terminal cleanup leaks/repeats.
- Coverage would require source changes or timeout injection.
</escalate_if>

<report_format>
Return exactly this, nothing else:

**Outcome:** done | blocked | escalated
**Changed:** file paths, or "none"
**Checks:** each command with its real result. Quote failing output verbatim.
Never report a check that was not run in this session.
**Blocker:** what stopped it, or "none"
**Next:** the card this unblocks, or what decision is needed
</report_format>
